export class Message {
    author: String
    message: String

    constructor(
        author: String,
        message: String
    ) {
        this.author = author
        this.message = message
    }
}